# ── Секреты — НИКОГДА не коммитить ──
.env
.env.*
!.env.example

# ── Python (bot, api) ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ── Node / Frontend ──
node_modules/
dist/
.vite/
*.local

# ── Vercel (локальная линковка проекта) ──
.vercel/

# ── Системное ──
.DS_Store
*.log

# ── Локальные бэкапы (страховка, не в репо) ──
*.bak
*.bak-*
